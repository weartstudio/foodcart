<div class="col-lg-6">
	<div class="shadow-sm card">
		<div class="card-body">
			<div class="row">
				<div class="col">

					<h4 class="h6 fw-semibold">
						<span>{{ product.title }}</span>
					</h4>

					<div class="text fs-xs">
						{{ product.content }}
					</div>

					<p class="price">
						<span>{{ product.price }}</span>
					</p>

				</div>

				<div class="col-lg-auto">
					<div class="position-relative">
						<img src="{{ product.thumbnail.src('thumbnail') }}" class="img-fluid" alt="Kép a(z) {{ post.title }}" style="max-width: 100px" />
						<button href="#" class="bottom-0 end-0 btn btn-light bi bi-plus position-absolute"></button>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>
